[package]
name = "simple-backend"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
inkwell = { version = "0.5", features = ["llvm18-0"], default-features = false }
